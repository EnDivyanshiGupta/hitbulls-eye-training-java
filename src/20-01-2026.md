# Car Java Program

## 📌 Description
This Java program demonstrates a simple class named `Car` with:
- Two instance variables: `brand` and `year`
- One method: `displayInfo()`
- A `main()` method to create an object and call the method

---

## 💻 Source Code

```java
class Car {
    String brand = "Toyoto";
    int year = 2023;

    public void displayInfo() {
        System.out.println("Brand: " + brand + ", Year: " + year);
    }

    public static void main(String[] args) {
        Car mycar = new Car();
        mycar.displayInfo();
    }
}
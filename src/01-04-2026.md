# Power of Three Solution

This is a Java implementation to determine if an integer is a power of three.

```java
class Solution {
    public boolean isPowerOfThree(int n) {
        if (n <= 0) return false;

        while (n % 3 == 0) {
            n /= 3;
        }

        return n == 1;
    }
}